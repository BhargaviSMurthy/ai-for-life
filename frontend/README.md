Minimal frontend scaffold

Run locally:

```powershell
cd frontend
npm install
npm start
```

This uses `lite-server` as a tiny static server for development. If you prefer React/Vite or another framework, tell me and I can scaffold that instead.
