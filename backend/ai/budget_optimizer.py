"""Budget optimizer utilities — placeholders for optimization routines."""


def optimize(budget, constraints=None):
    """Return a trivial allocation placeholder."""
    return {"allocated": budget, "constraints": constraints}
