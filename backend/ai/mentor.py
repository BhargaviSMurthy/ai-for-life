"""Mentor module — contains logic for conversational mentoring or guidance."""


def advise(context):
    """Return simple advice placeholder based on context."""
    return f"Advice based on context: {context}"
