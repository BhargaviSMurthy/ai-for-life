"""Authentication utilities (placeholder).

Replace with real auth (JWT/OAuth) as needed.
"""


def authenticate(token):
    """Dummy authenticate function — returns True if token equals 'dev-token'."""
    return token == "dev-token"
